// Modules
import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    principal: {
        paddingHorizontal: 25,
        marginBottom: 20,
    },
    text: {
        color: "#222B45",
        fontSize: 25,
        fontWeight: "bold"
    }
})
